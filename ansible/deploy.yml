- name: Final deployment configuration
  hosts: localhost
  connection: local
  tasks:
    - name: Print deployment status
      debug:
        msg: "React portfolio deployed on Kubernetes and running"

